{
  "name": "lab1ts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --no-source-maps --no-cache --no-content-hash",
    "coverage": "jest --coverage",
    "test": "jest --verbose"
  },
  "devDependencies": {
    "@types/jest": "29.5.12",
    "jest": "29.7.0",
    "parcel-bundler": "1.12.5",
    "ts-jest": "29.1.2",
    "typescript": "5.3.3"
  }
}
